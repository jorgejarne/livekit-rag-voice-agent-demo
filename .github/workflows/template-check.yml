# This workflow is disabled for this project as it's a real project, not a template
# uv.lock and livekit.toml are intentionally tracked in this repository

name: Template Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check-template-files:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Skip template check (not a template repo)
      run: echo "✓ This is a real project, not a template. Skipping template checks." 